datasource:
  db:
    driverClassName: org.postgresql.Driver
    username: sa
    password: sa
    url: jdbc:h2:mem:app

ebean:
  test:
    useDocker: false
    platform: h2
    ddlMode: dropCreate
    dbName: test
